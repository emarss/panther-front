import { ExchangeRateFlatDirective } from './exchange-rate-flat.directive';

describe('ExchangeRateFlatDirective', () => {
  it('should create an instance', () => {
    // const directive = new ExchangeRateFlatDirective();
    // expect(directive).toBeTruthy();
  });
});
