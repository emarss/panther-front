import { ExchangeRatePermissionDirective } from './exchange-rate-permission.directive';

describe('ExchangeRatePermissionDirective', () => {
  it('should create an instance', () => {
    // const directive = new ExchangeRatePermissionDirective();
    // expect(directive).toBeTruthy();
  });
});
