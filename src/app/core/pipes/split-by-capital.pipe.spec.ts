import { SplitByCapitalPipe } from './split-by-capital.pipe';

describe('SplitByCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
