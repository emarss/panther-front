import { LineBreakReplacePipe } from './line-break-replace.pipe';

describe('LineBreakReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
